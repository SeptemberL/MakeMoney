## 1. 数据库（账号库）与双引擎一致性

- [x] 1.1 盘点现有账号数据库初始化/迁移入口（MySQL + SQLite），定位新增表应放置的位置
- [x] 1.2 设计并新增日历项表（含 account_id、date、content、reminder_group、reminder_message、created_at、updated_at）
- [x] 1.3 为 `account_id + date` 增加索引（或等价优化），并确保 MySQL / SQLite 语义一致
- [x] 1.4 将建表/迁移逻辑同时落地到 MySQL 与 SQLite 两套实现，并在本地完成初始化验证

## 2. 后端服务与 API（CRUD）

- [x] 2.1 定义投资日历项的数据结构（实体/DTO）与参数校验规则（date 格式、content 非空、长度限制）
- [x] 2.2 实现“按日期范围查询（月范围）”接口，返回稳定排序结果
- [x] 2.3 实现“创建日历项”接口（绑定当前账号上下文，不允许越权）
- [x] 2.4 实现“更新日历项”接口（仅允许改业务字段；更新刷新 updated_at；越权拒绝）
- [x] 2.5 实现“删除日历项”接口（越权拒绝；删除后表现为不存在）

## 3. Web UI：投资日历页面（月视图 + 弹窗）

- [x] 3.1 新增投资日历页面入口（路由/菜单/导航）与基础模板文件
- [x] 3.2 实现月历 UI（上月/下月切换、日期网格、当日标记、带数据日期的视觉提示）
- [x] 3.3 实现日期弹窗：列表区（当日已添加项）+ 表单区（content/reminder_group/reminder_message）
- [x] 3.4 前端对接 API：加载整月数据；点击日期弹窗显示当日列表；新增/保存/删除后刷新 UI
- [x] 3.5 前端校验与错误提示：content 不能为空；date 必须合法；保存失败给出可理解提示

## 4. 验证与回归

- [x] 4.1 手工验证：跨月/跨年切换、无数据日新增、有数据日编辑/删除、同日多条排序稳定
- [x] 4.2 数据隔离验证：切换账号后不可看到/修改其他账号日历项（越权语义一致）
- [x] 4.3 双引擎验证：MySQL 与 SQLite 均可初始化建表并完成 CRUD 基本链路

