## 1. 代码定位与接入点确认

- [x] 1.1 盘点现有“筛选信号/指标”体系的入口：信号注册表、参数解析、调用链与返回结构
- [x] 1.2 明确信号命名与调用标识：`signal-meirenjian` 在配置/CLI/API 中的使用方式
- [x] 1.3 确认行情数据获取口径：日线 OHLCV、复权方式、缺失数据/停牌处理的现状

## 2. 核心算法实现（形态识别）

- [x] 2.1 实现 pivot 关键点提取（基于 `pivotLeft/pivotRight`），输出 pivot high/low 序列
- [x] 2.2 在 `lookbackBars` 窗口内搜索候选结构点 `LS/NL1/T/NL2/RS` 并验证时间顺序约束
- [x] 2.3 实现约束判定：肩高差、头部突出、颈线斜率、形态跨度（对应 spec）
- [x] 2.4 实现评分函数（0~1）与 `minScore` 命中逻辑，并输出 `reason`（失败路径）
- [x] 2.5 规范化输出：`hit/score/reason/points/neckline/window`

## 3. 与现有筛选/扫描框架集成

- [x] 3.1 将 `signal-meirenjian` 接入信号注册/路由体系（可被筛选器调用）
- [x] 3.2 实现参数注入与默认值：支持用户覆盖 spec 列出的参数
- [x] 3.3 保证批量扫描性能：窗口化计算、必要的预过滤与（可选）缓存

## 4. 测试与可验证性

- [x] 4.1 为 pivot 提取与关键约束判定编写单元测试（覆盖边界：数据不足、极端斜率、肩差过大等）
- [x] 4.2 增加至少 1 个“命中样例”和 1 个“非命中样例”的回归测试（固定输入 K 线片段）
- [x] 4.3 输出结构与字段校验测试：确保 `points/neckline/window` 格式稳定

## 5. 文档与示例

- [x] 5.1 补充使用示例：如何在筛选配置中启用 `signal-meirenjian` 并调整参数
- [x] 5.2 记录默认参数的设计意图与调参建议（误报/漏报的常见调法）

