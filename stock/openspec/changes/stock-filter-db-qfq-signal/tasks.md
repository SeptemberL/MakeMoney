## 1. 现状梳理与入口收敛

- [x] 1.1 定位 `stock_filter` 与信号检查系统中“线上数据/实时数据”参与信号计算的入口与调用链路，并列出需要改为 DB 读取的点位
- [x] 1.2 明确“信号计算默认数据源=DB”的约束边界（哪些场景允许实时临时计算），并在代码层加开关/参数以避免未来再次混用

## 2. 数据访问层：复权因子查询能力

- [x] 2.1 在数据库访问层新增/补齐“从 `adj_factor` 表按 `ts_code + trade_date(区间)` 批量读取前复权因子”的接口（支持批量股票与日期区间）
- [x] 2.2 为复权因子查询补充必要索引/查询优化（例如 `ts_code, trade_date`），并确保性能可接受
- [x] 2.3 明确复权因子缺失的判定规则与返回结构（完整覆盖/部分缺失/全缺失）

## 3. 复权转换：raw → 前复权序列

- [x] 3.1 实现“行情与复权因子按交易日对齐”的对齐函数（严格按 `trade_date` 一一匹配）
- [x] 3.2 实现前复权转换函数：将 `open/high/low/close` 从 raw 转换为前复权口径（并明确 `volume/amount` 的处理策略）
- [x] 3.3 增加缺失策略实现：默认 `insufficient_data` 跳过；可选 `raw_fallback` 回退但必须显式标注

## 4. 信号计算改造：以 `price-level-interval-signal` 为例

- [x] 4.1 改造 `price-level-interval-signal` 的“当前报价/比较逻辑”数据源为“DB 行情 + 前复权转换”
- [x] 4.2 在缺失复权因子场景下，按策略跳过或回退，并确保不会静默当作前复权继续计算
- [x] 4.3 在信号触发/未触发的输出或日志中增加口径元数据字段（`data_source`, `adjustment`, `factor_coverage` 等）

## 5. 批量筛选/扫描性能与缓存

- [x] 5.1 在单次任务执行周期内对复权因子做缓存（按股票+交易日或按区间），避免重复 DB 往返
- [x] 5.2 将行情与复权因子的读取改为批量查询路径（按股票集合与日期区间），并验证在全市场扫描场景下的耗时

## 6. 测试与回归

- [x] 6.1 为“交易日对齐 + 前复权转换”编写单元测试（包含对齐成功、对齐失败、因子缺失、回退策略）
- [x] 6.2 为 `price-level-interval-signal` 增加回归测试：同一输入下新链路口径一致、缺失策略可观测
- [x] 6.3 增加一次端到端验证：从 DB 装载 → 转换 → 触发/节流 → 输出口径字段正确

