## 1. 后端能力

- [x] 1.1 新增“缺失日线补齐”服务方法：遍历股票清单，查询每只股票最新入库日期，计算缺失区间并拉取回补
- [x] 1.2 新增触发接口（如设置模块下 `POST` 路由），返回执行汇总：总股票数、补齐股票数、新增记录数、失败明细
- [x] 1.3 增加容错与日志：单股票失败不中断，日志记录股票代码、时间区间与错误原因
- [x] 1.4 处理去重写入（复用现有 upsert/唯一键约束），避免重复数据污染

## 2. 设置页面集成

- [x] 2.1 在设置页面新增“补齐缺失日线数据”按钮
- [x] 2.2 按钮触发后端接口，增加禁用态/加载态，防止重复点击
- [x] 2.3 展示结果摘要（成功/失败数量、补齐数据条数），失败时给出可读提示

## 3. 验证

- [ ] 3.1 构造“最新日期落后多日”的股票样例，验证可补齐完整缺失区间
- [ ] 3.2 构造“已是最新数据”的股票样例，验证任务跳过且不重复写入
- [ ] 3.3 构造部分股票拉取失败场景，验证整体任务不中断且结果统计准确
- [ ] 3.4 在当前数据库类型（MySQL 或 SQLite）下完成一次端到端手工验证
