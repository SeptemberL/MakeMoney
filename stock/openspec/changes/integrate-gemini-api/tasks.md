## 1. 依赖与配置骨架

- [x] 1.1 在 `requirements.txt` 增加 Google Gen AI Python SDK（例如 `google-genai`）并确认与当前 Python/Flask 版本兼容
- [x] 1.2 在 `config.py` 增加读取 `[GEMINI]`（或约定节名）的 `model`、`timeout_seconds`、可选 `api_key` 占位；并明确环境变量 `GEMINI_API_KEY` / `GOOGLE_API_KEY` 的优先级高于配置文件
- [x] 1.3 在 `config.ini.example`（若仓库有示例）或内部部署说明中增加 Gemini 配置样例，**不包含**真实密钥

## 2. 客户端封装

- [x] 2.1 新增集中封装模块（例如 `Managers/gemini_client.py` 或 `gemini/client.py`），对外暴露 `generate_text(prompt: str, **kwargs) -> str` 或等价接口
- [x] 2.2 在封装内完成 SDK 初始化（读取密钥、默认模型、超时），未配置密钥时抛出明确异常
- [x] 2.3 实现超时、有限次重试与错误分类（鉴权/4xx 不重试；网络与 5xx 可退避重试）
- [x] 2.4 日志中避免打印完整密钥与用户敏感全文；必要时仅记录模型名、耗时与错误类型

## 3. 可验证性与文档

- [x] 3.1 为封装添加单元测试：使用 mock/stub 验证「无密钥失败」「超时/重试分支」「成功返回文本」路径（默认不依赖外网）
- [x] 3.2 （可选）增加受保护的管理/调试路由或 CLI 片段，仅在本地或管理员场景启用，用于人工验证连通性；默认关闭或需显式开关
- [x] 3.3 在变更目录或 `Docs/` 中补充最短「如何设置密钥与模型」说明（若项目惯例禁止新文档，可写在现有运维文档中）

## 4. 收尾

- [ ] 4.1 本地或预发环境用真实 Key 做一次冒烟调用，确认出站网络可达
- [x] 4.2 运行相关测试套件，确认无回归后准备合并
