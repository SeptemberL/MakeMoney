## 1. 回测核心模块

- [x] 1.1 新增回测 Python 包（如 `backtest/`），定义回测输入（标的、起止日期、策略标识或参数）与输出结构（收益曲线摘要、累计收益、最大回撤、交易列表可选）
- [x] 1.2 复用 `database/` 或现有行情读取接口加载 OHLCV，对齐索引与缺失数据处理策略
- [x] 1.3 实现至少一种可运行策略（如双均线或对接现有 `signals/` 之一），完成向量化或逐日循环回测逻辑
- [x] 1.4 为回测核心编写最小单元测试或脚本自测入口，覆盖正常区间与无数据分支

## 2. Web 路由与 API

- [x] 2.1 在 `routes.py`（或等价蓝图）注册回测页面路由（如 `/quant/backtest`）与 JSON API（如 `POST /api/quant/backtest`），参数校验与错误信息符合 spec
- [x] 2.2 页面请求调用 1.x 模块，API 返回结构化 JSON 供前端渲染

## 3. 模板与量化入口

- [x] 3.1 新增回测页面模板（与 Bootstrap/现有 `quant.html` 风格一致），包含表单、触发按钮与结果展示区
- [x] 3.2 修改 `templates/quant.html`：将「策略回测」卡片从「敬请期待」改为指向回测页面的链接或按钮

## 4. 文档与索引

- [x] 4.1 新增 `Docs/量化回测系统.md`：说明功能、操作步骤、数据假设与限制
- [x] 4.2 在回测页脚或 `Docs/项目基础框架.md`「页面路由」表中增加回测路径与文档引用，满足「可发现」要求

## 5. 数据库（仅当需要持久化时）

- [x] 5.1 若本期需要保存回测任务或结果，设计表结构并在 MySQL 与 SQLite 两侧同步建表/迁移；否则跳过并仅在文档注明「当前不落库」

