---
description: 在修改数据库表结构时，强制同时维护 MySQL 和 SQLite 的实现
alwaysApply: true
---

# 数据库引擎实现一致性规范

当**添加 / 修改 / 删除**数据库表或字段时，必须**同时**更新：

- MySQL 相关建表与迁移逻辑
- SQLite 相关建表与迁移逻辑

## 适用范围

- 修改任意数据库模型、迁移脚本、手写 SQL 时
- 涉及表结构（表、索引、字段、约束）的任何改动

## 执行指引

1. **先确定改动点**
   - 明确是新增表、修改字段、删除表/字段或索引调整。
2. **定位 MySQL 实现**
   - 找到当前项目中负责 MySQL 表结构的定义或迁移位置。
   - 完成相应的结构修改。
3. **定位 SQLite 实现**
   - 在项目中找到对应的 SQLite 表结构定义或迁移位置。
   - 做出与 MySQL **语义等价** 的修改（字段名、类型、默认值、约束语义需一致）。
4. **双引擎校验**
   - 检查两处实现是否包含相同的表、字段、索引与约束语义。
   - 如有差异，优先统一到业务期望的最终结构。

## 常见反例（不要这样做）

- 只在 MySQL 迁移文件中新增字段，却忘记在 SQLite 中同步。
- 在 SQLite 中删除表或字段，但 MySQL 中仍然保留，导致行为不一致。

## 推荐做法

- 在代码评审时，凡是看到涉及表结构变更的 MR/PR，**必须检查是否同时改了 MySQL 和 SQLite**。
- 如暂时只完成了一个引擎的修改，需在同一变更中补齐另一个引擎，而不是“以后再改”。
